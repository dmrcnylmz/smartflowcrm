# ============================================
# Callception — Service Reverse Proxy Config
# ============================================
# Replace DOMAIN with your actual domain (callception.com)
# SSL certs: Cloudflare Origin Certificate (15-year, zero maintenance)
# Place cert/key in ../ssl/origin.pem and ../ssl/origin-key.pem
# ============================================

# ---- Upstream Definitions ----
upstream kokoro {
    server kokoro:8880;
}

upstream n8n {
    server n8n:5678;
}

upstream context_api {
    server context-api:8999;
}

# ================================================
# tts.callception.com — Kokoro TTS
# ================================================
server {
    listen 443 ssl http2;
    server_name tts.callception.com;

    ssl_certificate     /etc/nginx/ssl/origin.pem;
    ssl_certificate_key /etc/nginx/ssl/origin-key.pem;

    # Rate limiting: 60 req/min per IP (burst of 20)
    limit_req zone=tts_limit burst=20 nodelay;

    # CORS for Vercel
    add_header Access-Control-Allow-Origin "https://callception.com" always;
    add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Authorization, Content-Type" always;

    # Preflight
    if ($request_method = 'OPTIONS') {
        add_header Access-Control-Allow-Origin "https://callception.com";
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
        add_header Access-Control-Allow-Headers "Authorization, Content-Type";
        add_header Access-Control-Max-Age 86400;
        return 204;
    }

    location / {
        proxy_pass http://kokoro;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # TTS responses can be large audio — disable buffering
        proxy_read_timeout 30s;
        proxy_buffering off;
    }

    location /health {
        proxy_pass http://kokoro/health;
        access_log off;
    }
}

# ================================================
# n8n.callception.com — Workflow Automation
# ================================================
server {
    listen 443 ssl http2;
    server_name n8n.callception.com;

    ssl_certificate     /etc/nginx/ssl/origin.pem;
    ssl_certificate_key /etc/nginx/ssl/origin-key.pem;

    # n8n UI (protected by n8n's own Basic Auth)
    location / {
        proxy_pass http://n8n;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support for n8n editor UI
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Webhook endpoints — open for Vercel callbacks
    location /webhook/ {
        limit_req zone=api_limit burst=30 nodelay;

        proxy_pass http://n8n;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /healthz {
        proxy_pass http://n8n/healthz;
        access_log off;
    }
}

# ================================================
# context.callception.com — Context API
# ================================================
server {
    listen 443 ssl http2;
    server_name context.callception.com;

    ssl_certificate     /etc/nginx/ssl/origin.pem;
    ssl_certificate_key /etc/nginx/ssl/origin-key.pem;

    # Rate limiting: 120 req/min per IP
    limit_req zone=api_limit burst=30 nodelay;

    # CORS for Vercel
    add_header Access-Control-Allow-Origin "https://callception.com" always;
    add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Authorization, Content-Type, X-API-Key" always;

    if ($request_method = 'OPTIONS') {
        add_header Access-Control-Allow-Origin "https://callception.com";
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
        add_header Access-Control-Allow-Headers "Authorization, Content-Type, X-API-Key";
        add_header Access-Control-Max-Age 86400;
        return 204;
    }

    location / {
        proxy_pass http://context_api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /health {
        proxy_pass http://context_api/health;
        access_log off;
    }
}

# ================================================
# HTTP → HTTPS Redirect
# ================================================
server {
    listen 80;
    server_name tts.callception.com n8n.callception.com context.callception.com;

    # Health check (for Nginx itself)
    location /health {
        return 200 'ok';
        add_header Content-Type text/plain;
    }

    # Redirect all HTTP to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}
